services:
  web:
    build: .
    image: ${APP_SLUG:-noonwake-test}:latest
    container_name: ${APP_SLUG:-noonwake-test}
    restart: unless-stopped
    ports:
      - "127.0.0.1:${APP_PORT:-3001}:80"
