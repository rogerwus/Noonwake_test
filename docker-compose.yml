services:
  web:
    image: nginx:alpine
    container_name: ${APP_SLUG:-noonwake-test}
    restart: unless-stopped
    ports:
      - "127.0.0.1:${APP_PORT:-3001}:80"
    volumes:
      - ./site:/usr/share/nginx/html:ro
